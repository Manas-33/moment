[pytest]
# Plain pytest (no pytest-django). Tests target pure helper functions only:
# no Django DB/settings, no network, no ffmpeg, no Claude API calls.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
